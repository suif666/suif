-- NPC 功能远程脚本（高亮 + 距离 + 血量 + 增大）
-- 专为 npcTab 设计，兼容主脚本延迟注入 Tabs

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local function waitForTabs()
    local Tabs = getgenv().Tabs
    while not Tabs do
        task.wait()
        Tabs = getgenv().Tabs
    end
    return Tabs
end

local function waitForNpcTab()
    local Tabs = waitForTabs()
    local tab = Tabs.npcTab
    while not tab do
        task.wait()
        Tabs = getgenv().Tabs
        tab = Tabs and Tabs.npcTab
    end
    return tab
end

local tab = waitForNpcTab()

local NPCSettings = {
    MaxHighlightDistance = 150,
    ShowHealthDisplay = true,
    NPCSizeMultiplier = 4,
    EnableNPCEnlarge = true
}

local stored = setmetatable({}, { __mode = "k" })
local activeNPCs = {}
local npcs = {}

local function getPlayerPosition()
    local char = LocalPlayer.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart") or char.PrimaryPart
        if hrp then
            return hrp.Position
        end
    end
    local cam = workspace.CurrentCamera
    if cam then
        return cam.CFrame.Position
    end
    return Vector3.new()
end

local function updateNPCVisibility(humanoid)
    if not humanoid or not humanoid.Parent then
        return
    end

    local character = humanoid.Parent
    if not character or Players:GetPlayerFromCharacter(character) then
        return
    end

    local playerPos = getPlayerPosition()
    local npcPos = character:GetPivot().Position
    local distance = (npcPos - playerPos).Magnitude
    local withinRange = distance <= NPCSettings.MaxHighlightDistance

    local highlight = character:FindFirstChildOfClass("Highlight")
    if withinRange then
        if not highlight then
            highlight = Instance.new("Highlight")
            highlight.FillTransparency = 1
            highlight.OutlineTransparency = 0
            highlight.Parent = character
        end
        highlight.Enabled = true
    elseif highlight then
        highlight.Enabled = false
    end

    local billboard = character:FindFirstChild("NPCHealthDisplay")
    if NPCSettings.ShowHealthDisplay and withinRange then
        if not billboard then
            billboard = Instance.new("BillboardGui")
            billboard.Name = "NPCHealthDisplay"
            billboard.Size = UDim2.new(4, 0, 1.5, 0)
            billboard.StudsOffset = Vector3.new(0, 2.5, 0)
            billboard.AlwaysOnTop = true

            local head = character:FindFirstChild("Head") or character.PrimaryPart or character:FindFirstChildWhichIsA("BasePart")
            billboard.Adornee = head

            local textLabel = Instance.new("TextLabel")
            textLabel.Size = UDim2.new(1, 0, 1, 0)
            textLabel.BackgroundTransparency = 1
            textLabel.Text = "HP: " .. tostring(math.floor(humanoid.Health))
            textLabel.Font = Enum.Font.SourceSansBold
            textLabel.TextSize = 18
            textLabel.TextColor3 = Color3.new(1, 1, 1)
            textLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
            textLabel.TextStrokeTransparency = 0.25
            textLabel.Parent = billboard

            billboard.Parent = character

            if not humanoid:GetAttribute("HealthConnected") then
                humanoid:GetPropertyChangedSignal("Health"):Connect(function()
                    local gui = character:FindFirstChild("NPCHealthDisplay")
                    if gui then
                        local label = gui:FindFirstChildOfClass("TextLabel")
                        if label then
                            label.Text = "HP: " .. tostring(math.floor(humanoid.Health))
                        end
                    end
                end)
                humanoid:SetAttribute("HealthConnected", true)
            end
        else
            billboard.Enabled = true
        end
    elseif billboard then
        billboard.Enabled = false
    end
end

local partsToEnlarge = {"Head", "HumanoidRootPart", "UpperTorso"}

local function enlargeNPC(obj)
    if not NPCSettings.EnableNPCEnlarge or not obj or not obj.Parent then
        return
    end

    local hum = obj:FindFirstChildOfClass("Humanoid") or obj:FindFirstChild("AnimationController")
    if not hum then
        return
    end

    for _, name in ipairs(partsToEnlarge) do
        local p = obj:FindFirstChild(name)
        if p and p:IsA("BasePart") then
            if not stored[p] then
                stored[p] = {
                    Size = p.Size,
                    Transparency = p.Transparency,
                    CanCollide = p.CanCollide
                }
            end

            p.Size = stored[p].Size * NPCSettings.NPCSizeMultiplier
            p.Transparency = 0.25
            p.CanCollide = false

            if not p:GetAttribute("RestoreHooked") then
                p:SetAttribute("RestoreHooked", true)
                p.Destroying:Once(function()
                    local orig = stored[p]
                    if orig and p then
                        p.Size = orig.Size
                        p.Transparency = orig.Transparency
                        p.CanCollide = orig.CanCollide
                    end
                    stored[p] = nil
                end)
            end
        end
    end
end

local function restoreAllNPCs()
    for part, orig in pairs(stored) do
        if part and part.Parent then
            part.Size = orig.Size
            part.Transparency = orig.Transparency
            part.CanCollide = orig.CanCollide
        end
    end
end

local function reEnlargeAllNPCs()
    for model in pairs(activeNPCs) do
        if model and model.Parent then
            enlargeNPC(model)
        end
    end
end

for _, obj in ipairs(workspace:GetDescendants()) do
    if obj:IsA("Model") and not Players:GetPlayerFromCharacter(obj) then
        local hum = obj:FindFirstChildOfClass("Humanoid") or obj:FindFirstChild("AnimationController")
        if hum then
            activeNPCs[obj] = true
            table.insert(npcs, hum)
            enlargeNPC(obj)
            updateNPCVisibility(hum)
        end
    end
end

workspace.DescendantAdded:Connect(function(obj)
    if obj:IsA("Model") and not Players:GetPlayerFromCharacter(obj) then
        local hum = obj:FindFirstChildOfClass("Humanoid") or obj:FindFirstChild("AnimationController")
        if hum then
            activeNPCs[obj] = true
            table.insert(npcs, hum)
            enlargeNPC(obj)
            updateNPCVisibility(hum)
        end
    end
end)

RunService.Heartbeat:Connect(function()
    for i = #npcs, 1, -1 do
        local hum = npcs[i]
        if not hum or not hum.Parent or Players:GetPlayerFromCharacter(hum.Parent) then
            table.remove(npcs, i)
        else
            updateNPCVisibility(hum)
        end
    end
end)

tab:CreateToggle({
    Title = "开启 NPC 高亮",
    Desc = "距离内显示高亮",
    Default = false,
    Callback = function(v)
        if not v then
            for _, model in pairs(activeNPCs) do
                if model and model.Parent then
                    local h = model:FindFirstChildOfClass("Highlight")
                    if h then
                        h.Enabled = false
                    end
                end
            end
        else
            for _, hum in ipairs(npcs) do
                updateNPCVisibility(hum)
            end
        end
    end
})

tab:CreateSlider({
    Title = "高亮距离",
    Min = 0,
    Max = 500,
    Default = NPCSettings.MaxHighlightDistance,
    Callback = function(Value)
        NPCSettings.MaxHighlightDistance = Value
    end
})

tab:CreateToggle({
    Title = "显示 NPC 血量",
    Default = NPCSettings.ShowHealthDisplay,
    Callback = function(Value)
        NPCSettings.ShowHealthDisplay = Value
        for _, model in pairs(activeNPCs) do
            if model and model.Parent then
                local gui = model:FindFirstChild("NPCHealthDisplay")
                if gui then
                    gui.Enabled = Value
                end
            end
        end
    end
})

tab:CreateToggle({
    Title = "启用 NPC 增大",
    Default = NPCSettings.EnableNPCEnlarge,
    Callback = function(Value)
        NPCSettings.EnableNPCEnlarge = Value
        if Value then
            reEnlargeAllNPCs()
        else
            restoreAllNPCs()
        end
    end
})

tab:CreateSlider({
    Title = "NPC 增大倍率",
    Min = 1,
    Max = 10,
    Default = NPCSettings.NPCSizeMultiplier,
    Callback = function(Value)
        NPCSettings.NPCSizeMultiplier = Value
        if NPCSettings.EnableNPCEnlarge then
            for part, data in pairs(stored) do
                if part and part.Parent then
                    part.Size = data.Size * Value
                end
            end
        end
    end
})

print("[NPC 功能] 已加载到 npcTab")