do
    local old = rawget(getgenv(), "__NPC_REMOTE_STATE")
    if old and old.cleanup then
        pcall(old.cleanup)
    end
end

local State = {}
getgenv().__NPC_REMOTE_STATE = State

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")

local NPCSettings = {
    EnableHighlight = false,
    MaxHighlightDistance = 150,
    ShowHealthDisplay = true,
    EnableNPCEnlarge = false,
    NPCSizeMultiplier = 4,          -- 默认值设为 4，确保滑块初始显示不为 0
    EnlargePartMode = "全部"
}

local stored = setmetatable({}, { __mode = "k" })
local activeNPCs = {}
local npcs = {}
local connections = {}
local uiBuilt = false

-- 记录上次实际应用的倍率，用于 Heartbeat 检测变化
local lastAppliedScale = NPCSettings.NPCSizeMultiplier

local enlargeTargets = {
    ["头部"] = {"Head"},
    ["身体"] = {"HumanoidRootPart"},
    ["躯干"] = {"UpperTorso", "LowerTorso", "Torso"},
    ["全部"] = {"Head", "HumanoidRootPart", "UpperTorso", "LowerTorso", "Torso"}
}

-- ---------- 辅助函数 ----------
local function getPlayerPosition()
    local char = LocalPlayer.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart") or char.PrimaryPart
        if hrp then
            return hrp.Position
        end
    end
    local cam = workspace.CurrentCamera
    if cam then
        return cam.CFrame.Position
    end
    return Vector3.new()
end

local function getOrCreateBillboard(character, humanoid)
    local billboard = character:FindFirstChild("NPCHealthDisplay")
    if billboard then
        return billboard
    end

    billboard = Instance.new("BillboardGui")
    billboard.Name = "NPCHealthDisplay"
    billboard.Size = UDim2.new(4, 0, 1.5, 0)
    billboard.StudsOffset = Vector3.new(0, 2.5, 0)
    billboard.AlwaysOnTop = true
    billboard.Enabled = false

    local head = character:FindFirstChild("Head") or character.PrimaryPart or character:FindFirstChildWhichIsA("BasePart")
    billboard.Adornee = head

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = "HP: " .. tostring(math.floor(humanoid.Health))
    label.Font = Enum.Font.SourceSansBold
    label.TextSize = 18
    label.TextColor3 = Color3.new(1, 1, 1)
    label.TextStrokeTransparency = 0.25
    label.Parent = billboard

    billboard.Parent = character
    return billboard
end

local function updateNPCVisibility(humanoid)
    if not humanoid or not humanoid.Parent then
        return
    end

    local character = humanoid.Parent
    if not character or Players:GetPlayerFromCharacter(character) then
        return
    end

    local ok, npcPos = pcall(function()
        return character:GetPivot().Position
    end)
    if not ok then
        return
    end

    local distance = (npcPos - getPlayerPosition()).Magnitude
    local withinRange = distance <= NPCSettings.MaxHighlightDistance

    local highlight = character:FindFirstChildOfClass("Highlight")
    if NPCSettings.EnableHighlight and withinRange then
        if not highlight then
            highlight = Instance.new("Highlight")
            highlight.FillTransparency = 1
            highlight.OutlineTransparency = 0
            highlight.Parent = character
        end
        highlight.Enabled = true
    elseif highlight then
        highlight.Enabled = false
    end

    local billboard = character:FindFirstChild("NPCHealthDisplay")
    if NPCSettings.ShowHealthDisplay and withinRange then
        billboard = billboard or getOrCreateBillboard(character, humanoid)
        if billboard then
            billboard.Enabled = true
        end
    elseif billboard then
        billboard.Enabled = false
    end
end

-- ---------- 体型放大核心 ----------
local function getTargetParts(obj)
    local list = enlargeTargets[NPCSettings.EnlargePartMode] or enlargeTargets["全部"]
    local result = {}
    for _, name in ipairs(list) do
        local p = obj:FindFirstChild(name)
        if p and p:IsA("BasePart") then
            result[#result + 1] = p
        end
    end
    return result
end

-- 存储并放大单个NPC（用于初次注册或部位切换）
local function enlargeNPC(obj)
    if not NPCSettings.EnableNPCEnlarge or not obj or not obj.Parent then
        return
    end

    for _, p in ipairs(getTargetParts(obj)) do
        if not stored[p] then
            stored[p] = {
                Size = p.Size,
                Transparency = p.Transparency,
                CanCollide = p.CanCollide
            }
        end
        p.Size = stored[p].Size * NPCSettings.NPCSizeMultiplier
        p.Transparency = 0.25
        p.CanCollide = false
    end
end

-- 恢复所有部件至原始尺寸（不清除stored）
local function restoreAllNPCs()
    for part, orig in pairs(stored) do
        if part and part.Parent then
            part.Size = orig.Size
            part.Transparency = orig.Transparency
            part.CanCollide = orig.CanCollide
        end
    end
end

-- 更新所有已存储部件的尺寸（根据当前倍率），并记录最后应用的倍率
local function updateEnlargeScale()
    if not NPCSettings.EnableNPCEnlarge then
        return
    end
    local mult = NPCSettings.NPCSizeMultiplier
    for part, orig in pairs(stored) do
        if part and part.Parent then
            part.Size = orig.Size * mult
            part.Transparency = 0.25
            part.CanCollide = false
        end
    end
    lastAppliedScale = mult
end

-- 重新选择放大部位（部位切换或开关开启时调用）
local function refreshEnlargeParts()
    restoreAllNPCs()
    stored = setmetatable({}, { __mode = "k" })
    if not NPCSettings.EnableNPCEnlarge then
        return
    end
    for model in pairs(activeNPCs) do
        if model and model.Parent then
            enlargeNPC(model)
        end
    end
    lastAppliedScale = NPCSettings.NPCSizeMultiplier   -- 同步
end

-- ---------- 注册与清理 ----------
local function registerNPC(obj)
    if not obj or not obj:IsA("Model") then
        return
    end
    if Players:GetPlayerFromCharacter(obj) then
        return
    end

    local hum = obj:FindFirstChildOfClass("Humanoid") or obj:FindFirstChild("AnimationController")
    if not hum then
        return
    end

    if not activeNPCs[obj] then
        activeNPCs[obj] = true
        npcs[#npcs + 1] = hum
    end

    enlargeNPC(obj)
    updateNPCVisibility(hum)
end

local function cleanup()
    for _, c in ipairs(connections) do
        pcall(c.Disconnect, c)
    end
    connections = {}

    restoreAllNPCs()

    local folder = CoreGui:FindFirstChild("NPC_REMOTE_FOLDER")
    if folder then
        folder:Destroy()
    end

    stored = setmetatable({}, { __mode = "k" })
    activeNPCs = {}
    npcs = {}
    uiBuilt = false
end

State.cleanup = cleanup

-- ---------- 等待主界面Tab ----------
local function waitForNpcTab(timeout)
    timeout = timeout or 20
    local start = tick()
    repeat
        local Tabs = getgenv().Tabs
        if Tabs and Tabs.npcTab then
            return Tabs.npcTab
        end
        task.wait(0.1)
    until tick() - start > timeout
    return nil
end

local tab = waitForNpcTab(20)
if not tab then
    return
end

-- ---------- 初始化已有NPC ----------
for _, obj in ipairs(workspace:GetDescendants()) do
    registerNPC(obj)
end

-- ---------- 连接事件 ----------
connections[#connections + 1] = workspace.DescendantAdded:Connect(function(obj)
    task.defer(function()
        registerNPC(obj)
    end)
end)

-- Heartbeat 循环：更新可见性 + 检测倍率变化并应用
connections[#connections + 1] = RunService.Heartbeat:Connect(function()
    -- 处理可见性
    for i = #npcs, 1, -1 do
        local hum = npcs[i]
        if not hum or not hum.Parent or Players:GetPlayerFromCharacter(hum.Parent) then
            table.remove(npcs, i)
        else
            updateNPCVisibility(hum)
        end
    end

    -- 检测倍率是否变化，若变化则应用新倍率（无耗时遍历模型，仅遍历已存储部件）
    if NPCSettings.EnableNPCEnlarge and NPCSettings.NPCSizeMultiplier ~= lastAppliedScale then
        updateEnlargeScale()
    end
end)

-- ---------- 构建UI ----------
if not uiBuilt then
    uiBuilt = true

    local function safe(f)
        local ok, err = pcall(f)
        if not ok then
            warn("[NPC 功能] UI 创建失败: " .. tostring(err))
        end
    end

    safe(function()
        tab:Toggle({
            Title = "开启 NPC 高亮",
            Value = NPCSettings.EnableHighlight,
            Callback = function(v)
                NPCSettings.EnableHighlight = v
            end
        })
    end)

    safe(function()
        tab:Slider({
            Title = "ESP 范围",
            Min = 0,
            Max = 500,
            Default = NPCSettings.MaxHighlightDistance,
            Callback = function(v)
                NPCSettings.MaxHighlightDistance = v
            end
        })
    end)

    safe(function()
        tab:Toggle({
            Title = "显示 NPC 血量",
            Value = NPCSettings.ShowHealthDisplay,
            Callback = function(v)
                NPCSettings.ShowHealthDisplay = v
            end
        })
    end)

    safe(function()
        tab:Toggle({
            Title = "启用 NPC 增大",
            Value = NPCSettings.EnableNPCEnlarge,
            Callback = function(v)
                NPCSettings.EnableNPCEnlarge = v
                task.spawn(refreshEnlargeParts)   -- 异步执行，不阻塞UI
            end
        })
    end)

    safe(function()
        tab:Slider({
            Title = "NPC 增大倍率",
            Min = 1,
            Max = 10,
            Default = NPCSettings.NPCSizeMultiplier,   -- 默认 4，确保滑块初始不是 0
            Callback = function(v)
                NPCSettings.NPCSizeMultiplier = v
                -- 不做任何其他操作，由 Heartbeat 自动检测并应用
            end
        })
    end)

    safe(function()
        tab:Dropdown({
            Title = "放大部位",
            Multi = false,
            Values = {"头部", "身体", "躯干", "全部"},
            Value = NPCSettings.EnlargePartMode,
            Callback = function(v)
                NPCSettings.EnlargePartMode = v
                task.spawn(refreshEnlargeParts)   -- 异步执行
            end
        })
    end)

    safe(function()
        tab:Button({
            Title = "恢复 NPC 正常体型",
            Callback = function()
                NPCSettings.EnableNPCEnlarge = false
                restoreAllNPCs()
                -- 可选：同步 Toggle 状态（主脚本可能需要额外处理，此处略）
            end
        })
    end)
end